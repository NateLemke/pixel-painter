import React from "react";
import LoginHeader from "./loginHeader";

export default function LoginPage() {
    return(
        <div id="loginContainer" className="w-full h-full">
            <LoginHeader/>
        </div>
    );
}